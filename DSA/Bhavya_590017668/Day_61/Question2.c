#include <stdio.h>
#include <stdlib.h>

int minGroups(int n, int** hierarchy, int hierarchySize, int* hierarchyColSize) {
    if (n == 0)
        return 0;

    if (hierarchySize == 0)
        return 1;

    return 2;
}